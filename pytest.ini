[pytest]
testpaths = tests
addopts =
    -ra
    --strict-markers
    --cov=artnet_blaze
    --cov-report=term-missing
    --cov-fail-under=85
filterwarnings =
    error
    ignore::ResourceWarning
    ignore::pytest.PytestUnraisableExceptionWarning
